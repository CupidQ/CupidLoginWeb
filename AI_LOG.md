# AI 辅助记录

记录日期：2026-09-17 至 2026-09-18。工具：Codex。以下是本次对话已发生的事实4

### 关键对话


> 当光标保持静止、背景粒子靠近并被吸附时，连线出现闪烁和抽动。排查这个特定交互状态下的更新逻辑并修复。另外，昼夜切换时顶部遮罩的颜色直接跳变，补上平滑过渡。

**AI 排查后的答复摘要：**

原库在粒子与光标的距离平方达到 `max / 2` 时，才突然施加位置修正。粒子可能在自由漂移与吸附之间反复跨越阈值；绘制过程中又会修改粒子位置，导致同一帧中的连线端点不同步。顶部遮罩则直接替换 `linear-gradient` 中的颜色，没有实现两层渐变之间的交叉过渡。

> 给出修复过程

**AI 随后解释的修复过程：**

1. 保留官方 `canvas-nest.js` 文件，通过 `SmoothCanvasNest` 继承并替换绘制方法。
2. 用连续的距离权重代替吸附硬阈值：先计算靠近程度 `p`，再使用 `p * p * (3 - 2 * p)` 平滑变化吸附强度。鼠标作用范围仍为 60px。
3. 越靠近光标，越弱化自由漂移，让粒子逐渐靠拢；把每帧流程改成“更新所有位置 → 清空画布 → 绘制所有粒子和连线”。
4. 让连线透明度在作用范围边缘趋近于零，并根据帧间隔调整位移，减少突现和刷新率差异。
5. 顶部和底部各使用白天、黑夜两层固定渐变，通过 1.2 秒透明度过渡实现交叉淡入淡出。

### 调试与验证记录

- 第一轮模拟检查未通过：初始吸引系数 `0.012` 在测试位置不足以抵消向外漂移。AI 将系数调整为 `0.025` 后重新测试，而不是把第一次实现直接当作完成。
- 在 60Hz、120Hz 下各模拟静止光标吸附 20 秒，测试粒子均平稳收敛，没有往返振荡；最大单帧位移分别约为 0.3214px、0.1607px。
- JavaScript 语法检查通过。浏览器确认粒子画布存在、遮罩透明度正在变化，控制台未发现错误。
- 验证范围：以上是特定粒子初始条件的模拟与当前浏览器检查，不代表已经覆盖所有设备、光标轨迹及粒子分布。

**这段提问体现的做法：** 本人指出了“静止光标 + 吸附状态”这一触发条件，并区分了连线抖动与遮罩跳变两种现象；修复后进一步要求解释过程。

## 调整校验时机，而不只调整页面外观

**提问**

> 空白输入框仅仅获得焦点再失去焦点，就立即提示必填，交互过于敏感。请调整错误提示的触发时机，让用户开始填写前不会被过早打断。

- 本人指出的问题：错误提示出现得太早，影响填写体验。
- AI 的实现：空白且没有已有错误的输入框在失焦时保持安静；非空或已经报错的输入框继续校验；提交时仍检查全部必填项。
- 后续重构时的回归检查：空白输入框切换焦点后没有错误提示；点击登录后，账号和密码仍出现必填提示。
- 这段提问体现的做法：把“不好用”具体化为“什么操作后，不应出现什么反馈”。

## 在功能完成后，明确提出可读性与保持行为的约束



**提问**

> 请在不改变现有功能、样式和动画效果的前提下整理源码。按职责拆分逻辑、统一排版，只保留解释关键原因的注释，让初学者能顺着代码理解执行过程。

- AI 的调整：Go 后端拆分注册、登录和账号查询；前端集中事件绑定，拆出具名交互函数；粒子代码分离位置更新和绘制；HTML/CSS 展开排版。第三方库保持原样。
- 保持外观的证据：对比重构前后的文件，CSS 规则顺序和所有值未改变，HTML 标签、属性和文案未改变。
- 保持粒子行为的证据：在相同初始条件下对比重构前后连续 1200 帧的粒子坐标与绘制指令，结果一致。
- 其他检查：`go test ./...`、`go vet ./...` 和 JavaScript 语法检查通过；浏览器检查了表单切换、密码显示、空白失焦、提交校验、登录失败 alert 和昼夜切换。
- 这段提问体现的做法：提出了明确的重构边界——提高可读性，同时要求保持已有行为。

## 其他迭代记录（历史版本，不代表当前最终配置）

2026-09-18：本人要求粒子随昼夜渐变、切换时模拟地球自转光线变化，以及太阳/月亮和密码眼睛动画。AI 添加 1 秒粒子 RGB 插值、1.2 秒弧形光影扫过、SVG 月牙遮罩与太阳光芒过渡、眼睛斜线描画和瞳孔缩放；保留 60px 鼠标作用范围。减少动态效果模式下跳过动画。

2026-09-18：指定 https://github.com/hustcc/canvas-nest.js/releases 。AI接入官方 2.0.1 UMD 文件，下载时校验 npm 发布包 SHA-512，保留 MIT 许可证；设置淡蓝连线、桌面 99 个点/窄屏 45 个点，在后台或减少动态效果模式下销毁动画。浏览器已确认 canvas 显示、爱心元素为零且控制台无错误。